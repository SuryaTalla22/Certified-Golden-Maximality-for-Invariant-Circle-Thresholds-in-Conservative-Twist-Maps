# Theorem III Source of Truth

The consumed theorem-facing Theorem III artifact is:

```text
artifacts/final_discharge/stage_cache/theorem_iii.json
```

In the current proof line, Theorem III is closed by a TrackB direct lower-anchor persistence certificate, not by the older lower-corridor/mesh-chain object. The certificate proves the direct lower anchor

```text
K = 0.971635
```

for downstream strict comparison. It explicitly records:

```text
certificate_kind = direct_lower_anchor_persistence_certificate
track = TrackB
theorem_facing = true
passed = true
promotion_allowed = true
failed_checks = []
```

The audit/replay layer therefore treats Theorem III as a direct lower-anchor artifact. It does not infer or claim a full mesh corridor or a nontrivial parameter interval unless a separate theorem-facing object supplies that stronger statement.

The strict direct-anchor audit is generated by:

```bash
python scripts/replay_heavy_lower.py --mode regenerate-heavy --no-figures
```

This writes the canonical direct-anchor audit files under:

```text
artifacts/proof_audit/lower_corridor/lower_direct_anchor_audit.json
artifacts/proof_audit/lower_corridor/lower_direct_anchor_audit.bundle.json
```

and also writes legacy-compatible aliases:

```text
artifacts/proof_audit/lower_corridor/lower_anchor_closure_audit.json
artifacts/proof_audit/lower_corridor/lower_anchor_closure_audit.bundle.json
artifacts/proof_audit/lower_corridor/lower_corridor_audit.json
artifacts/proof_audit/lower_corridor/lower_corridor_audit.bundle.json
```

Those aliases exist only so older replay entry points can consume the new direct-anchor payload. Their payload explicitly records `lower_anchor_mode = direct-lower-anchor` and excludes both a mesh-corridor claim and a parameter-interval claim.
